# FULMAR dependencies — Python 3.11
# Install with: pip install -r requirements.txt

astropy>=5.3
astroquery>=0.4.6
lightkurve>=2.4
matplotlib>=3.7
numpy>=1.24
pytransit>=2.5
pyyaml>=6.0
wotan>=1.10
ldtk>=1.7

# transitleastsquares — install the Geert Jan Talens fork manually (see README):
# pip install git+https://github.com/talensgj/tls.git